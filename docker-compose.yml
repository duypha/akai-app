version: '3.8'

services:
  akai:
    build: .
    container_name: akai
    ports:
      - "3000:8000"
    env_file:
      - .env
    volumes:
      - akai-data:/app/backend/data  # Persist SQLite database
    restart: unless-stopped

volumes:
  akai-data:
    driver: local
